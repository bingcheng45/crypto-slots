import SlotMachine from '@/components/SlotMachine'

export default function Home() {
  return (
    <main className="min-h-screen p-8 flex items-center justify-center" style={{ backgroundColor: '#0f0f0f' }}>
      <SlotMachine />
    </main>
  )
}
